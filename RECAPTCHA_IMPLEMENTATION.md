# reCAPTCHA v3 スパム対策実装完了

## 実装日時
2025年10月31日

## 実装概要
angels-healing.comの通常サイトのお問い合わせフォームにGoogle reCAPTCHA v3を実装し、スパムメール対策を行いました。

## 対象ファイル

### 1. contact.php (バックエンド検証)
- **変更内容**: reCAPTCHA v3トークンの検証ロジックを追加
- **機能**:
  - ユーザーポータルからのリクエストは除外（別システムのため）
  - 通常サイトからのフォーム送信時にreCAPTCHAトークンを検証
  - Google APIにトークンを送信してスコアを取得
  - スコア0.5未満の場合はスパムと判定してエラーページへリダイレクト
  - 検証結果をエラーログに記録

### 2. index.html (メインページのフォーム)
- **変更内容**: reCAPTCHA v3スクリプトとフォーム送信処理を追加
- **機能**:
  - Google reCAPTCHA v3ライブラリを読み込み
  - フォーム送信時に自動的にreCAPTCHAトークンを取得
  - 取得したトークンを隠しフィールドに設定してPHP側に送信
  - ユーザーには何も操作させない（v3の特徴）

### 3. simple-contact.html (シンプルフォーム)
- **変更内容**: reCAPTCHA v3スクリプトとフォーム送信処理を追加
- **機能**: index.htmlと同様

## 設定情報

### reCAPTCHA v3 認証情報
- **サイトキー**: `6LeocfwrAAAAADHsRETBOJwQa3hrrrnB7pQeVjC`
- **シークレットキー**: `6LeocfwrAAAAAG8PCr97naTbztmkv32BQTga8kCp`
- **スコア閾値**: 0.5（0.5以上を人間と判定）

### スコアの意味
- **1.0**: ほぼ確実に人間
- **0.5**: 人間である可能性が高い（推奨閾値）
- **0.0**: ほぼ確実にボット

## ユーザーエクスペリエンス
- reCAPTCHA v3は**完全に透過的**に動作します
- ユーザーはチェックボックスをクリックしたり、画像を選択したりする必要がありません
- フォーム送信時に自動的にバックグラウンドで検証が行われます

## 除外対象
- **ユーザーポータル** (`user-portal/`) からのお問い合わせは別システムのため、reCAPTCHA検証を適用していません
- これにより、既存のポータル機能に影響を与えることなく、通常サイトのみスパム対策が有効になります

## テスト方法

### 1. 正常なフォーム送信テスト
1. `index.html` または `simple-contact.html` にアクセス
2. フォームに適切な情報を入力
3. 送信ボタンをクリック
4. ブラウザの開発者ツールのコンソールで以下のメッセージを確認:
   - "reCAPTCHA検証を実行中..."
   - "reCAPTCHAトークン取得完了。フォームを送信します。"
5. `thanks.html` にリダイレクトされることを確認

### 2. サーバーログの確認
- PHPエラーログに以下の情報が記録されます:
  - 成功時: `reCAPTCHA passed. Score: [スコア], IP: [IPアドレス]`
  - 失敗時: `reCAPTCHA verification failed. Score: [スコア], IP: [IPアドレス]`

### 3. スパムボット対策の確認
- reCAPTCHAスコアが0.5未満の場合、`form-error.html` にリダイレクトされます
- 自動化されたスパム送信は高確率でブロックされます

## 期待される効果
- **1日1700通以上のスパムメール**を大幅に削減
- 正常なユーザーには影響なし（透過的な動作）
- サーバーログでスパム試行を監視可能

## 注意事項
- reCAPTCHAのキーは本番環境用のドメイン（angels-healing.com）に登録されています
- ローカル開発環境でテストする場合、GoogleのreCAPTCHA管理画面でlocalhostドメインを追加する必要があります
- スコア閾値（現在0.5）は、スパムの状況に応じて調整可能です（0.3〜0.7が一般的）

## トラブルシューティング

### フォームが送信できない場合
1. ブラウザの開発者ツールでJavaScriptエラーを確認
2. reCAPTCHAスクリプトが正しく読み込まれているか確認
3. サーバーのPHPエラーログを確認

### スパムが依然として届く場合
- `contact.php`のスコア閾値を0.5から0.6または0.7に引き上げることを検討
- ただし、閾値を上げすぎると正常なユーザーもブロックされる可能性があるため注意

## 今後の改善案
- reCAPTCHA v3のスコア統計をダッシュボードで確認し、最適な閾値を決定
- スパム試行の統計を記録して傾向を分析
- 必要に応じて、特定のIPアドレスやユーザーエージェントの追加フィルタリング

---

## 実装完了
すべての通常サイトのお問い合わせフォームにreCAPTCHA v3が実装され、スパム対策が有効になりました。
